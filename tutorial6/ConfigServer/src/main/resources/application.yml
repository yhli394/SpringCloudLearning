spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yhli394/SpringCloudLearning
          search-paths: configcenter
          timeout: 10
          refresh-rate: 5
      label: master


server:
  port: 8888
