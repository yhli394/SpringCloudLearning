spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yhli394/SpringCloudLearning
          search-paths: configcenter
      label: master


server:
  port: 8888
